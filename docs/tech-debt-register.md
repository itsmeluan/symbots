# Tech Debt Register

- **2026-07-15** (Part-DB Story 002): GDD↔TR drift — `design/gdd/part-database.md` Rule 1 + story AC name **4** reserved Full-Vision fields; `TR-part-025` and the shipped `PartDef` schema carry **6** (`+critical_output, +firewall`). Code follows the TR (source of truth). Fix = align the GDD/story text to 6. Tracked from `production/epics/part-database/story-002-partdef-schema-enums-catalog.md`
- **2026-07-15** (Part-DB Story 003): AC-14 literal-`null` contract vs Godot 4.7 — a literal `null` passed to the `StringName`-typed `get_part(id)` is statically type-rejected at the call boundary ("Cannot convert argument 1 from Nil to StringName"), never reaching the body. Kept ADR-0003's `StringName` typing (user decision); the null contract is carried by the `&""` null-equivalent (Story 002 `&""=none`). No fix required — documented so future callers know to pass `&""`, never `null`. Tracked from `production/epics/part-database/story-003-partdb-loader.md`
- **2026-07-15** (Part-DB Story 003): LogSink base created without a home story — `src/core/diagnostics/log_sink.gd` (`@abstract`, ADR-0002 §5) was implemented as a Story-003 prerequisite (the DI `load_catalog(catalog, log_sink)` signature needs it). It belongs to ADR-0002, not the Part-DB epic. Fix = confirm it's referenced/owned when the EventBus/diagnostics story lands. Tracked from `production/epics/part-database/story-003-partdb-loader.md`
- **2026-07-15** (Part-DB Story 003): stale engine label — story-003 Context reads "Godot 4.6" (line 20) while the project is pinned 4.7. Folds into the pending 4.6→4.7 ADR/architecture-doc re-validation sweep (`/architecture-review`). Tracked from `production/epics/part-database/story-003-partdb-loader.md`
- **2026-07-15** (Part-DB Story 003 → blocks Story 010 CI): new `class_name` scripts (e.g. `LogSink`) must be present in `.godot/global_script_class_cache.cfg` before a headless GUT run can resolve `extends <NewClass>`. If that cache is gitignored, Story 010's CI workflow must regenerate it (`godot --headless --editor --quit`) as a pre-test step, else `extends LogSink` fails to resolve in CI. Tracked from `production/epics/part-database/story-003-partdb-loader.md`
- **2026-07-15** (Part-DB Story 004): `StatMath` + `BalanceConfig` born without a dedicated home story — the first ADR-0005 stat-formula story created the shared Layer-1 primitive (`src/core/stats/stat_math.gd`) and the Layer-4 tuning Resource (`src/core/stats/balance_config.gd`). Both are ADR-0005 infra that Stories 005/006/007+ extend. Fix = confirm ownership/coverage as those stories land (esp. that `BalanceConfig` fields stay append-only). Tracked from `production/epics/part-database/story-004-upgrade-formula-f2-f2b.md`
- **2026-07-15** (Part-DB Story 004 → boot/validator/Story 010): `assets/data/balance_config.tres` is NOT authored yet — unit tests inject `BalanceConfig.new()` (GDD-default multipliers). Downstream must (a) author the `.tres`, (b) load it at BootScreen step 2b (ADR-0005/0004), (c) add the ContentValidator BalanceConfig section asserting `upgrade_multipliers` == GDD table within safe ranges. Tracked from `production/epics/part-database/story-004-upgrade-formula-f2-f2b.md`
- **2026-07-15** (Part-DB Story 004): stale engine label — story-004 Context reads "Godot 4.6" (line 20). Same 4.6→4.7 re-validation sweep as story-003. Tracked from `production/epics/part-database/story-004-upgrade-formula-f2-f2b.md`
- **2026-07-15** (Part-DB Story 006): `drop_rate_by_rarity` appended to `BalanceConfig` — ADR-0005 Layer 4 enumerates stat-pipeline tables (upgrade/xp/chassis/type-chart); drop-rate config is a new field-family added there under the "single BalanceConfig, reject per-system configs" decision. When the ContentValidator BalanceConfig section is built (Story 007+), it must assert `drop_rate_by_rarity[BOSS_GRADE] == 0.001` (never 0.0 — multiplicative-inert) and the other rarities match the GDD table. Also: `assets/data/balance_config.tres` (still unauthored, from Story 004) must include this field. Tracked from `production/epics/part-database/story-006-drop-rate-formula-f3.md`
- **2026-07-15** (Part-DB Story 006): `DropRateFormula` is ADR-0003-governed but homed in `src/core/stats/` (ADR-0005's designated "pure formula core"). Non-blocking placement note — revisit if a dedicated Drop System module home emerges in that epic. Tracked from `production/epics/part-database/story-006-drop-rate-formula-f3.md`
- **2026-07-15** (Part-DB Story 006): stale engine label — story-006 Context reads "Godot 4.6" (line 20). Same 4.6→4.7 sweep. Tracked from `production/epics/part-database/story-006-drop-rate-formula-f3.md`
- **2026-07-15** (Part-DB Story 007): `ContentCatalogs` aggregate born without a home story — `src/core/content/content_catalogs.gd` (`class_name ContentCatalogs extends RefCounted`) was created because the `validate(catalogs, log_sink)` signature (ADR-0003 §5) needs a loaded-catalog aggregate, but only `PartCatalog` exists so far (Move/Passive/Consumable/Enemy DBs unstoried). Same born-in-story pattern as `LogSink` (003) and `StatMath`/`BalanceConfig` (004). Fix = confirm ownership + APPEND-ONLY field growth as those DB stories land; Stories 008/009 read its other slots for cross-DB families. Tracked from `production/epics/part-database/story-007-validator-schema-family.md`
- **2026-07-15** (Part-DB Story 007 → NEEDS USER CONFIRMATION): `damage_type` validation gating — GDD AC-21 lists `damage_type` among always-required MVP enums, but `damage_type` is skill-delivered, so a skill-less part (any Core; a Common with no active skill) legitimately carries the unset `0`. Shipped interpretation: reserved values (DATA/TRUE) rejected on EVERY part; a valid MVP `damage_type` (PHYSICAL/ENERGY) required ONLY when `active_skill_id != &""`. This avoids false-positives on valid skill-less parts. Confirm against the GDD's intent; if every part (incl. skill-less) must name a damage_type, tighten `_check_damage_type` in `content_validator.gd`. Tracked from `production/epics/part-database/story-007-validator-schema-family.md`
- **2026-07-15** (Part-DB Story 007): reserved-element diagnostic reuses the generic `content_invalid_element` code rather than a distinct "reserved-value" code (unlike `content_reserved_damage_type`, which is distinct). The AC only requires the value be flagged; distinct-code granularity for CRYO/CORROSIVE/DATA is a low-value nicety Stories 008/009 can add when extending the validator. Tracked from `production/epics/part-database/story-007-validator-schema-family.md`
- **2026-07-15** (Part-DB Story 007): stale engine label — story-007 Context reads "Godot 4.6" (line 20). Same 4.6→4.7 sweep. Tracked from `production/epics/part-database/story-007-validator-schema-family.md`
- **2026-07-15** (Part-DB Story 005 → blocks Story 010 .tres authoring): `chassis_modifiers` appended to `BalanceConfig` as an UNTYPED nested `Dictionary` (outer key `PartDef.ChassisArchetype` → inner `Dictionary[StringName→float]`). Story 001 verified the `.tres` round-trip only for `Dictionary[StringName, int]`; a nested Dictionary of per-stat float dicts is NOT yet round-trip-verified. When `assets/data/balance_config.tres` is authored (Story 010), verify the nested table serializes+reloads intact on 4.7 — OR keep the table code-default-only and have the ContentValidator assert it against the GDD in code. Tracked from `production/epics/part-database/story-005-total-stat-formula-f1.md`
- **2026-07-15** (Part-DB Story 005 → future ContentValidator balance section): `chassis_modifiers` is stored SPARSE (only non-×1.0 entries; absent stats/archetypes resolve to ×1.0 via `.get`). Faithful to the GDD, but the validator's BalanceConfig section (Story 008+ / whoever owns the balance-table checks) should assert the stored entries equal the GDD Formula 1 table AND that no unexpected stat/archetype key has drifted in. Joins the Story 004/006 note that the validator must check `upgrade_multipliers` and `drop_rate_by_rarity`. Tracked from `production/epics/part-database/story-005-total-stat-formula-f1.md`
- **2026-07-15** (Part-DB Story 005): stale engine label — story-005 Context reads "Godot 4.6" (line 20). Same 4.6→4.7 sweep. Tracked from `production/epics/part-database/story-005-total-stat-formula-f1.md`
- **2026-07-15** (Part-DB Story 008 → ADR-0003/ADR-0005 boundary, confirm): the AC-12/AC-23 validation bounds (`stat_budgets`, `primary_stat_common_caps`, `primary_stat_rare_floors`) were appended to `BalanceConfig` (ADR-0005 "single BalanceConfig, reject per-system configs") and are read via DI (`ContentCatalogs.balance`), satisfying the story's "source it from config, not hardcoded". The content families gate on `_cfg != null`, so they run only when a config is injected (Story 007's schema-only fixtures inject none and stay green). Structural maps (`ELEMENT_TAGS`, `PRIMARY_STAT`, `MANUFACTURER_TAGS`) + fixed design thresholds (`MAX_SINGLE_STAT=55`, `BOSS_BREAK_MIN_MULTIPLIER=500.0`, `CONCENTRATION_MIN=0.70`) remain validator constants — they are not tuning knobs. Confirm this config-vs-constant split is the intended ADR-0003/0005 boundary. Tracked from `production/epics/part-database/story-008-validator-content-budget-family.md`
- **2026-07-15** (Part-DB Story 008 → joins Story 004/005/006 balance-section notes): the three new `BalanceConfig` tables are nested/untyped Dictionaries (`stat_budgets`: SlotType→Rarity→`[min,max]`; the two caps/floors: SlotType→int) with GDD-default `@export` values. When `assets/data/balance_config.tres` is authored (Story 010), the nested `stat_budgets` round-trip must be verified on 4.7 (same open question as `chassis_modifiers`), and the ContentValidator balance section should assert all six tables (`upgrade_multipliers`, `drop_rate_by_rarity`, `chassis_modifiers`, `stat_budgets`, `primary_stat_common_caps`, `primary_stat_rare_floors`) equal the GDD. Tracked from `production/epics/part-database/story-008-validator-content-budget-family.md`
- **2026-07-15** (Part-DB Story 008): stale engine label — story-008 Context reads "Godot 4.6" (line 20). Same 4.6→4.7 sweep. Tracked from `production/epics/part-database/story-008-validator-content-budget-family.md`
- **2026-07-15** (Part-DB Story 009 → reconcile when Move/Passive DB epics land): AC-13 referential resolution uses two new APPEND-ONLY `ContentCatalogs` slots — `move_ids`/`passive_ids` as `{StringName: true}` membership sets, gated by `references_mounted: bool`. No `MoveCatalog`/`PassiveCatalog` class was built (Move/Passive DB epics unstoried + out of scope). The real boot must populate these sets from the loaded Move/Passive DBs; when those epics define real catalog classes with `has_skill()`/`has_passive()`, reconcile the id-set seam (either keep the boot-populated set, or have `ContentCatalogs` hold the real catalogs and the validator call their `has_*`). ADR-0003-compliant (StringName IDs, DI) and consistent with the Story 007 `ContentCatalogs`-born-without-home precedent. Tracked from `production/epics/part-database/story-009-validator-referential-level-fields.md`
- **2026-07-15** (Part-DB Story 009 → NEEDS USER CONFIRMATION): `level_requirement == 0` semantics — the validator treats 0 as the unset sentinel (→ defaults to 1), so a non-Common part left at 0 FAILS its higher rarity floor (RARE 3 / Boss 6 / Proto 8). Effect: Rare+ parts must author an explicit `level_requirement`; a COMMON at 0 passes (floor 1). Matches TR-part-011 "never go below floor" + QA AC-2. Confirm this authoring-gate strictness is intended (vs. 0 meaning "auto-use the rarity floor"). Tracked from `production/epics/part-database/story-009-validator-referential-level-fields.md`
- **2026-07-15** (Part-DB Story 009 → scope drift, Story 010 or follow-up): `PartDef` doc comments attribute `drop_conditions` and `upgrade_effects` entry-shape validation to "Story 009" (lines 145, 165), but Story 009's ACs cover only AC-13 + `level_requirement`/`level_growth`. Entry-shape checks for those two Array[Dictionary] fields are NOT yet implemented anywhere. Either add them in Story 010's authoring/CI pass or a dedicated follow-up, and fix the stale PartDef comments. Tracked from `production/epics/part-database/story-009-validator-referential-level-fields.md`
- **2026-07-15** (Part-DB Story 009): stale engine label — story-009 Context reads "Godot 4.6" (line 20). Same 4.6→4.7 sweep. Tracked from `production/epics/part-database/story-009-validator-referential-level-fields.md`
